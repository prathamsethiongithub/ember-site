{
  "name": "ember-site",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "ember — the island. a minecraft launcher launch site. evidence read from the repo at deploy time.",
  "scripts": {
    "sync": "node scripts/sync-evidence.mjs",
    "serve": "node scripts/serve.mjs",
    "deploy": "npm run sync && git add -A && git commit -m \"deploy: evidence refresh\" && git push"
  }
}
